/**
 * @return string draw|computer|human
 * @param actionKey ChifoumiActions
 */
function determineWinner(actionKey) {
  console.log(actionKey);
  const computerChoice = '';

  return '';
}

export default determineWinner;